plugin_cache_dir = "/REDACTED_ABS_PATH"

provider_installation {
  filesystem_mirror {
    path    = "/REDACTED_ABS_PATH"
    include = ["local/providers/pyvider"]
  }

  direct {
    exclude = ["local/providers/pyvider"]
  }
}
